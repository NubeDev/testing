# Project Notes

This repository is a lazybones workflow target: a small todo application that pairs an Express REST backend (list, create, and delete todos) with a minimal static frontend that consumes it, alongside backend unit tests and Playwright end-to-end tests. The current worktree also carries a minimal Rust crate (`testing`) so that `cargo test --workspace` succeeds.
